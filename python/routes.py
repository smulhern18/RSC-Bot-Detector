
def estimate_likelihood(username: str):
    return
    # pull data for the username and the trained parameters for the ML
    # then go and run the RSC model


def train(training_dataset: str, testing_dataset: str, name_of_parameters: str):
    return
    # Pull the training and testing datasets, then save the parameters in a json file
